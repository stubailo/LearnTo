- if count == 1
  = post_comment.user.login + " responded to your post in " + post_comment.post.forum.class_room.name
  %br
- else
  = count + " users responded to your post in " + post_comment.post.forum.class_room.name
  %br