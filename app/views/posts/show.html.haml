= link_to "<- Back to forum", [@class_room, @forum]

.forum_post_container
  .forum_post
    = render :partial => "post", :locals => { :post => @post, :forum => @forum }
  .forum_post_responses
    - (@post.comments.sort { |c1, c2| c2.rating <=> c1.rating }).each do |comment|
      = render :partial => "comments/comment", :locals => {:comment => comment}
    .post_response_form
      = render :partial => "comments/newComment", :locals => { :post => @post }
  - if @post.user_id == current_user.id
    .no_javascript
      %h2 Edit your post:
      = render :partial => "posts/edit_post", :locals => {:post_id => @post.id}
